{% extends 'base.html.twig' %}

{% block body %}

{% block content %}
  <div class="text-right">
    <a href="{{ path('klient_dodaj') }}" class="btn btn-info" role="button">
      <span class="glyphicon glyphicon-file" aria-hidden="true"> </span>&nbsp;Dodaj
    </a>
  </div>
  <table class="table">
    <thead>
    <tr>
      <th class="text-center">#</th>
      <th class="text-center">Imię i nazwisko</th>
      <th class="text-center">Adres</th>
      <th class="text-center">Miasto</th>
      <th class="text-center">Nr telefonu</th>
    </tr>
    </thead>
    <tbody>
    {% for klient in klienci %}
      <tr class="info">
        <td class="text-center">{{ klient.id }}</td>
        <td class="text-center">{{ klient.imie }} {{ klient.nazwisko }}</i></td>
        <td class="text-center">{{ klient.adres }}</td>
        <td class="text-center">{{ klient.miasto }}</td>
        <td class="text-center">{{ klient.telefon }}</td>
        <td class="text-center">
          <a href="/klient/edytuj/{{ klient.id }}">
            <span class="glyphicon glyphicon-pencil" aria-hidden="true"></span>
          </a>
        </td>
        <td class="text-center">
          <a href="/remove/klient/{{ klient.id }}">
            <span class="glyphicon glyphicon-trash" aria-hidden="true"></span>
          </a>
        </td>
      </tr>
    {% endfor %}
    </tbody>
  </table>
{% endblock %}

{% endblock %}

{% block stylesheets %}

{% endblock %}
