{% extends 'base.html.twig' %}

{% block body %}

{% block content %}
    <div class="text-right">
        <a href="{{ path('programista_dodaj') }}" class="btn btn-info" role="button">
            <span class="glyphicon glyphicon-file" aria-hidden="true"> </span>&nbsp;Dodaj
        </a>
    </div>
<table class="table">
    <thead>
    <tr>
        <th class="text-center">#</th>
        <th class="text-center">Imię i nazwisko</th>
        <th class="text-center">Adres</th>
        <th class="text-center">Miasto</th>
        <th class="text-center">Technologia</th>
    </tr>
    </thead>
    <tbody>
    {% for p in programisci %}
    <tr class="info">
        <td class="text-center">{{ p.id }}</td>
        <td class="text-center">{{ p.imie }} {{ p.nazwisko }}</i></td>
        <td class="text-center">{{ p.adres }}</td>
        <td class="text-center">{{ p.miasto }}</td>
        <td class="text-center">{{ p.technologia.nazwa }}</td>
        <td class="text-center">
            <a href="/programista/edytuj/{{ p.id }}">
                <span class="glyphicon glyphicon-pencil" aria-hidden="true"></span>
            </a>
        </td>
        <td class="text-center">
            <a href="/remove/programista/{{ p.id }}">
                <span class="glyphicon glyphicon-trash" aria-hidden="true"></span>
            </a>
        </td>
    </tr>
    {% endfor %}
    </tbody>
</table>
{% endblock %}

{% endblock %}

{% block stylesheets %}

{% endblock %}
